apiVersion: v1
kind: Service
metadata:
  name: {{ include "implicaction.fullname" . }}
  labels:
    {{- include "implicaction.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "implicaction.selectorLabels" . | nindent 4 }}
